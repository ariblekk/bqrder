# Local compose files hold real secrets/values — commit only the examples.
docker-compose.yml
docker-compose.override.yml

# Local env files — commit only .env.example.
.env
.env.*
/be/qrdigo-067-firebase-adminsdk-fbsvc-ff7954bb38.json
!.env.example
